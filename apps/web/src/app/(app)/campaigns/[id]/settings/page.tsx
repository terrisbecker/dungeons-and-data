import { ComingSoon } from "../coming-soon";

export default function CampaignSettingsPage() {
  return (
    <ComingSoon
      title="Settings"
      description="Renaming the campaign, changing its status, and managing the roster are not exposed from the UI yet."
    />
  );
}
